import type { TypeGeneratePath } from '../../types'

export interface TypeCreateSnippetOptions extends TypeGeneratePath {
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  formattedSnippet: any
}
